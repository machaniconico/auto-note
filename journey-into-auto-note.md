# Journey Into auto-note

*claude-mem の永続メモリに記録された全 109 観測（13 セッション、2026-06-11 〜 2026-06-14 UTC）から再構成した開発史ナラティブ*

> タイムスタンプはすべて UTC 保存値。ローカル（JST, +9h）では「6/11 夜 → 6/12」「6/13 深夜 → 6/14」に対応する。本文では UTC を基準に追う。

---

## 1. プロジェクトの起源 (Project Genesis)

メモリに残る最初の足跡は **2026-06-11 20:45 UTC**、`feature/ui-design-polish` ブランチ上での UI 刷新作業から始まる。プロジェクト自体（`auto-note` — Python/Tkinter 製のデスクトップ記事管理アプリ）はこれより前から存在していたが、claude-mem が観測を記録し始めたのはこの「見た目の磨き込み」フェーズからだった。

最初の数分間で立て続けに記録された観測群が、このプロジェクトの性格をよく表している:

- **#11897** UI Density Settings with Three Accessibility Presets — 3 段階のアクセシビリティ・プリセット
- **#11898** Comprehensive Tkinter Widget Styling via `_configure_style` Method
- **#11899** Refined Color Palette with Teal Accent Theme — ティール基調のカラーパレット
- **#11900** Enhanced Font Handling for Japanese UI with Fallback Chain — 日本語 UI のフォント・フォールバック連鎖
- **#11901** Readability Metrics Guards for Safe Display Mode Detection

ここから読み取れる初期ビジョンは明快だ。**日本語ユーザー向けの、読みやすさとアクセシビリティを重視したローカル GUI アプリ**。クラウドでもなく、Web でもない。ユーザーの手元で動き、プライバシーと商用利用（"commercial readiness"）を意識する。解こうとしていた問題は派手な新機能ではなく、「すでに動くアプリを、人に見せて使ってもらえる品質まで引き上げる」ことだった。

創成期の技術的決定として象徴的なのが **#11903 Smoke Test Implementation for UI Rendering Validation**。GUI を持つアプリでありながら、ヘッドレスでレンダリングを検証する仕組みを早期に入れている。これは後の全フェーズで効いてくる伏線になる。

## 2. アーキテクチャの進化 (Architectural Evolution)

このプロジェクトの 4 日間は、アーキテクチャの「方向転換」というより **一貫した一つの方向への深掘り** だった。その方向とは——

**同期的に重い処理を、レイヤーをまたいで一度だけ計算し、UI スレッドから追い出す。**

進化を 3 つの局面で追える:

1. **見た目の層 (6/11)** — `_configure_style` による集中的なスタイリング、密度プリセット、フォント・フォールバック。UI の「表現」を一箇所に集約するリファクタが入った（#11898, #11916）。

2. **計算の重複を発見する層 (6/13)** — パフォーマンス監査で、アーキテクチャ上の構造的欠陥が次々に露わになる。`refresh_home()` がパネル更新のたびにアプリ全状態を再計算する (#12351)、自己テストが受け入れチェックごとに 2 回走る (#12359)、診断レポート生成がプライバシー監査と quickstart を 15 回以上キャッシュなしで再実行する (#12363)。これらは「機能の追加」ではなく「同じ計算を何度もしている」という**重複の発見**だった。

3. **重複を排除する層 (6/14)** — そして残りの大半の時間が、この重複を潰す作業に充てられた。`load_article`/`review_path` の mtime キー・メモ化 (#12378)、`FirstRunReport` への `SelfTestReport` フィールド追加による自己テスト結果の共有 (#12441)、診断レポートのリクエストスコープ重複排除 (#12477)、共有レポートを一度だけ計算して各セクションへ渡す設計 (#12482)。

アーキテクチャ上の最大の転換点は **#12441 / #12482 の「事前計算したレポートをオプション引数で下層へ渡す」パターンの導入**だ。後方互換（デフォルト `None`）を保ちつつ、上位レイヤーが計算済みオブジェクトを注入できるようにした。これにより「各コンポーネントが自前で重い計算をやり直す」というボトムアップの無駄を、トップダウンの共有で解消した。

## 3. 重要なブレークスルー (Key Breakthroughs)

トーンが「調査」から「解決」へ転じた瞬間がいくつかある。

- **#12350 / #12351 (6/13 23:25)** — 13 ターゲットの敵対的検証付き監査ワークフローを起動した直後、最も高価な発見が生まれた。`refresh_home()` がパネル更新のたびに全状態を再計算しているという根本原因 (#12351, discovery 68,117 トークン)。これがこのプロジェクトで「最も高くついた一つの気づき」であり、その後のすべての最適化の起点になった。

- **#12364 (6/13 23:36)** — 監査完了。**45 件の確定バグ/パフォーマンス問題**を auto-note 全体から洗い出した。混沌とした「なんとなく重い」が、数えられる作業リストに変わった瞬間。

- **#12439 (6/14 01:49)** — *Defer initial data load to idle callback to fix UI freeze on launch*。起動時のフリーズを、初期データロードを idle コールバックへ遅延させるだけで解消。小さな差分（discovery わずか 571t）で体感が劇的に変わる、典型的な「アハ」。

- **#12488 (6/14 02:19)** — 商用準備チェックボタンのクリックでフリーズする問題を、release-check ボタンが既に使っていたワーカースレッド・パターンを流用して解決。「すでにコードベース内にある正解パターンを再利用する」という、最も気持ちのいい類の解決。

## 4. 作業のリズム (Work Patterns)

タイムスタンプの密度が、開発のリズムをそのまま描き出している。

- **6/11 20:45–20:55（探索＋機能スプリント）** — 10 分間に 20 観測。UI 機能を一気に積み上げる高密度スプリント。Codex に視覚デザインの磨き込みを委譲 (#11915, #11916) しつつ、最後に 1 件のテスト退行を踏む。
- **6/11 → 6/13 の空白** — 約 2 日間、観測なし。アプリを実際に触り、次の課題（重さ）を体に溜める沈黙の期間。
- **6/13 23:21–23:48（探索フェーズ）** — 監査スプリント。27 分で 20 観測、しかしそのほとんどが `discovery` 型。コードを変えずに問題を見つける純粋な調査フェーズ。
- **6/14 00:49–02:19（デバッグ＆リファクタの連続バッチ）** — ここが本番。1 時間半で 68 観測。Batch 1〜9 という明確なバッチ構造で、「実装 → テスト → コミット → プッシュ」を 10 回繰り返した。

型別の内訳（全 109 観測）が作業の質を裏付ける:

| 型 | 件数 | 比率 |
|----|------|------|
| discovery（発見） | 53 | 49% |
| change（変更） | 20 | 18% |
| bugfix（バグ修正） | 18 | 17% |
| feature（機能） | 12 | 11% |
| decision（決定） | 4 | 4% |
| refactor（リファクタ） | 2 | 2% |

**観測のほぼ半分が `discovery`** という事実が、この 4 日間の本質を物語る。これは新機能を量産したプロジェクトではなく、**既存コードを理解し、無駄を見つけ、それを丁寧に潰した**プロジェクトだった。

## 5. 技術的負債 (Technical Debt)

負債の蓄積と返済が、同じタイムライン上にきれいに並んでいる。

**蓄積の痕跡**は監査フェーズに集中している。`refresh_home()` の全再計算 (#12351)、自己テストの二重実行 (#12359)、診断レポートの 15 回超の再実行 (#12363)、商用準備チェックの 3 回同期実行 (#12362)。これらはおそらく機能を素早く足していく過程で「とりあえず毎回計算する」素朴な実装が積み重なった結果だ。

**返済**は Batch 1〜9 として体系的に行われた。特筆すべきは、返済が場当たりでなく**監査 → 仕様書 → 実装 → 検証**の規律で進んだこと:

- #12403 Batch 2 仕様書を作成してから実装
- #12419 Batch 3 のデータ安全性修正の実装仕様書を先に作成
- #12476 Batch 8 の重複排除計画を立ててから着手

返済の質を担保したのが **オラクル検証** (#12479, #12481) だ。診断レポートの重複排除が「バイト単位で挙動を変えていない」ことを、一時ディレクトリパスを正規化したオラクルで確認した。負債返済の最大のリスク（=挙動を壊す）を、機械的な等価性チェックで封じている。

## 6. 困難とデバッグの長編 (Challenges and Debugging Sagas)

最も苦労した一連が **Batch 3 のフィールド検証退行** だ。

商用セットアップと「次のフィールド選択」のバリデーションを揃えるリファクタ (#12423) を入れたところ、アクションプラン生成で退行が発生した (#12426)。原因究明 (#12427) で判明したのは、**「未入力（空）」と「入力済みだが不正」を区別していたセマンティクスを、リファクタが取り違えた**こと。修正 (#12428, discovery 11,620t) でこの分離を復元し、テストが通った (#12429)。「整合性を取るための統一」が、実は意味のある区別を消してしまう——リファクタの古典的な落とし穴を踏んで、抜け出した。

プラットフォーム固有の苦労も繰り返し現れた。WSL/Linux 環境では **tkinter が利用不可** (#11911)、**pytest が venv に未導入** (#11909, #11910)、**Python コマンド自体が無い** (#11906)。GUI アプリを Linux 上の CI 的環境で検証する難しさが、随所でスモークテストやヘッドレス初期化 (#12435) の工夫を生んだ。

もう一つの地味な強敵が **エンコーディング**。BOM 由来の誤った変更検知 (#12361)、プライバシースキャンでの NUL バイトと CP932 フォールバック (#12421)。日本語・Windows・ローカルファイルという組み合わせ特有の、見えにくい不具合群だった。

## 7. メモリと継続性 (Memory and Continuity)

このプロジェクトでは claude-mem の継続性が実際に効いた場面が観測できる。6/11 の UI 作業と 6/13 の監査の間には約 2 日のギャップがあり、13 のセッションに作業が分割されている。明示的なリコール（narrative に「recalled / previous session」等を含む観測）は 2 件と少ないが、より重要なのは **passive injection**——各セッション開始時に直近の観測が文脈として注入されることで、「前回どのバッチまで終わったか」「テストが何件で何秒だったか」を再調査せずに継続できた点だ。

象徴的なのは、テスト件数とランタイムが観測をまたいで一貫して追跡されていること: 135 → 162 → 164 件、31 秒 → 22.8 秒 → 21.4 秒。これらの数字がセッション間で引き継がれたからこそ、「ベースラインからの改善」を毎バッチで主張できた (#12444, #12483)。

## 8. トークン経済学とメモリ ROI (Token Economics & Memory ROI)

claude-mem データベースへの直接クエリに基づく定量分析。

**基礎指標:**

| 指標 | 値 |
|------|----|
| 総 discovery トークン（全作業の原価） | **518,292 t** |
| 観測あたり平均 discovery | 4,755 t |
| 観測あたり平均 read（メモリ読み出し原価） | 393 t |
| **圧縮比（discovery : read）** | **約 12.1 : 1** |
| 総観測数 / セッション数 | 109 / 13 |
| 投資した read トークン総量 | 約 42,770 t |

圧縮比 12:1 の意味は明快だ。**一度 4,755 トークンかけて発見した知見を、次に思い出すコストは平均 393 トークン**——原価の約 8% で再利用できる。

**最も高価な観測トップ 5（システム内で最も価値の高いメモリ）:**

| 順位 | ID | discovery_t | タイトル |
|------|-----|------------|---------|
| 1 | #12351 | 68,117 | refresh_home() が全状態を毎回再計算 |
| 2 | #12352 | 68,117 | 13ターゲット監査ワークフロー起動 |
| 3 | #12359 | 15,259 | 自己テストが受け入れチェックごとに2回実行 |
| 4 | #12360 | 15,259 | テキストウィジェットの monospace フォントが更新ごとに消失 |
| 5 | #12361 | 15,259 | BOM エンコーディングによる誤変更検知 |

トップ 2 が突出している。これは敵対的検証付きの大規模監査という「一度やれば二度とやり直したくない」高コストの調査であり、まさにメモリが再実行を防いでくれる最高価値の資産だ。

**月次内訳（プロジェクトは単月に収まる）:**

| 月 | 観測 | discovery 合計 | セッション |
|----|------|---------------|-----------|
| 2026-06 | 109 | 518,292 t | 13 |

**ROI 推定:**

- 明示的リコール節約: 2 件 × 約 10K t = **約 20,000 t**
- パッシブ・リコール節約（スキル既定式: `(13-1) セッション × 50観測窓 × 平均4,755t × 0.30`）= **約 855,900 t**
- スキル式に基づく純 ROI = `(855,900 + 20,000) / 42,770` ≈ **20.5 倍**

ただし正直に注記する: パッシブ式の「50 観測窓」は本プロジェクト（総 109 観測）では窓同士が大きく重複するため、上振れしている。総 discovery を上限とした保守的な見積もり（`518,292 × 0.30 + 20,000 = 175,488 t`）では純 ROI ≈ **4.1 倍**。**実態はこの 4〜20 倍のレンジ**にあると読むのが誠実だ。いずれにせよ、約 43K トークンのメモリ投資が、数十万トークン規模の再調査を防いでいる。

## 9. タイムライン統計 (Timeline Statistics)

- **期間**: 2026-06-11 20:45 UTC 〜 2026-06-14 02:19 UTC（約 2 日 6 時間のウォールクロック、実作業は 3 セッション・スプリント）
- **総観測数**: 109 / **セッション数**: 13
- **型別内訳**: discovery 53、change 20、bugfix 18、feature 12、decision 4、refactor 2
- **最も活発な日**: 6/14（UTC）が 68 観測・232,312 discovery トークンで最大。次いで 6/13 が 20 観測ながら 229,068 トークン（監査が高コスト）、6/11 が 21 観測・56,912 トークン
- **最長のデバッグ集中**: 6/14 00:49–02:19 の連続バッチ処理（68 観測 / 90 分）
- **ホットパス**: `src/auto_note/gui.py` が圧倒的（プロジェクトメモリ上 222 回参照）、次いで `diagnostics.py`、`quickstart.py`

## 10. 教訓とメタ観察 (Lessons and Meta-Observations)

全史から浮かび上がる繰り返しのテーマ:

1. **「重い」の正体はほぼ常に重複だった。** 新しいアルゴリズムが必要だったのではなく、同じ計算を何度もしていた。`refresh_home` の全再計算、自己テストの二重実行、診断レポートの 15 回再実行、商用チェックの 3 回同期実行——すべて「一度だけ計算して共有する」で解けた。新しい開発者がこのコードベースから最初に学ぶべきは、**最適化の前にまず重複を数えろ**ということ。

2. **挙動保存の規律がリファクタを安全にした。** バイト単位のオラクル検証 (#12481)、毎バッチでのフルテスト実行、後方互換のオプション引数（デフォルト `None`）。大胆な内部変更を、外から見て何も変わらない形で入れる技術が一貫していた。

3. **UI スレッドを神聖視する。** 起動時ロードの idle 遅延 (#12439)、商用チェックのワーカースレッド化 (#12488)。Tkinter の `after_idle`/`after(ms)`/スレッドの実行モデル (#12462) を正確に理解したうえで、重い処理を main スレッドから追い出すパターンが繰り返された。

4. **監査 → 仕様 → 実装 → 検証の規律。** 45 件の問題を一度に見つけ (#12364)、バッチに分け、各バッチで仕様書を先に書いてから実装した。混沌を、数えられ・追跡でき・検証できる単位に変換する規律が、4 日間で 10 コミットという安定したペースを生んだ。

5. **日本語・Windows・ローカルの三重苦は地雷原。** BOM、CP932、UTC vs ローカル時刻、tkinter 不在の Linux CI。プラットフォーム固有の落とし穴が継続的に現れ、そのたびにエンコーディングと時刻の扱いを堅牢化していった。

このプロジェクトの物語を一文で言うなら——**「動くアプリを、速く・安全に・人に見せられる品質へ磨き上げる」ための、規律ある重複排除の記録**である。

---

*生成: claude-mem timeline-report / 分析対象 109 観測（2026-06-11 〜 2026-06-14 UTC）*

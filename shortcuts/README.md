# shortcuts

GUIに集約する前に作った個別起動ファイル置き場です。

普段はプロジェクト直下の `auto-note.lnk` を使ってください。文字や行高が潰れて見える場合は `auto-note safe display.lnk` を使います。

インストール後は、デスクトップまたはスタートメニューの `auto-note` と `auto-note safe display` から同じように起動できます。

ここにある `.bat` は必要なときだけ直接起動できます。
